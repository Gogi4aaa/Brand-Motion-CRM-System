-- ============================================================================
-- Линк към суровия материал (Google Drive папка от снимачния ден). Подава се
-- веднъж при импорта на сценарии и се записва върху всяко видео от партидата
-- — така монтажистът стига до footage-а директно от модала на видеото си.
-- ============================================================================

alter table content_items add column if not exists footage_url text not null default '';
