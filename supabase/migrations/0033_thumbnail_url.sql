-- ============================================================================
-- Google Drive линк за преглед на thumbnail-ите на всяко видео. Отделен е от
-- footage_url (суровия материал) — тук се качват/сменят готовите обложки.
-- Всеки член на екипа може да го добавя и сменя (същите RLS права като за
-- останалите полета на content_items).
-- ============================================================================

alter table content_items add column if not exists thumbnail_url text not null default '';
