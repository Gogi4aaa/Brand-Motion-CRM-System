-- ============================================================================
-- „Готов вариант": линк към финалната (монтирана) версия на всяко видео. Слага се
-- след преглед; оттам се тегли/копира готовият клип и се ползва при публикуване.
-- ============================================================================

alter table content_items add column if not exists final_url text not null default '';
